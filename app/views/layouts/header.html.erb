<div id="header">
    <nav class="navbar navbar-default navbar-static-top">
        <div class="container">
            <div class="page-header">
                <div class="row">
                    <div class="col-xs-6">
                        <h1>Group Site</h1>
                    </div>
                    <div class="col-xs-6" style="margin-top: 20px;">
                        <% if user_signed_in? %>
                        <span class="pull-right">
                            <strong><%= current_user.email %></strong><br/>
                        </span>
                        <br/>
                        <span class="pull-right">
                            <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %>
                            |
                            <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
                        </span>
                        <% end %>
                    </div>
                </div>
            </div>
            <% if user_signed_in? %>
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li class="<%= 'active' if params[:action] == 'forum_index' %>"><%= link_to("Forum", {:controller => '/posts', :action => 'forum_index'}) %></li>
                    <li class="<%= 'active' if params[:action] == 'text_chat' %>"><%= link_to("Chat", {:controller => '/pages', :action => 'text_chat'}) %></li>
                    <li class="<%= 'active' if params[:action] == 'drive' %>"><%= link_to("Drive", {:controller => '/pages', :action => 'drive'}) %></li>
                    <li class="<%= 'active' if params[:action] == 'video_chat' %>"><%= link_to("Video Chat", {:controller => '/pages', :action => 'video_chat'}) %></li>
                </ul>
            </div>
            <% end %>
        </div>
    </nav>
</div>
