<div id="header">
    <nav class="navbar navbar-default navbar-static-top">
        <div class="container">
            <div class="page-header">
                <h1>Group Site</h1>
            </div>
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li class="<%= 'active' if params[:action] == 'forum' %>"><%= link_to("Forum", {:controller => 'pages', :action => 'forum'}) %></li>
                    <li class="<%= 'active' if params[:action] == 'text_chat' %>"><%= link_to("Chat", {:controller => 'pages', :action => 'text_chat'}) %></li>
                    <li class="<%= 'active' if params[:action] == 'drive' %>"><%= link_to("Drive", {:controller => 'pages', :action => 'drive'}) %></li>
                    <li class="<%= 'active' if params[:action] == 'video_chat' %>"><%= link_to("Video Chat", {:controller => 'pages', :action => 'video_chat'}) %></li>
                </ul>
            </div>
        </div>
    </nav>
</div>
